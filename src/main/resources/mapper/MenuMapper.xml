<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wenzhuo4657.blog.admin.dao.MenuMapper">



    <sql id="Base_Column_List">
        id,menu_name,parent_id,
        order_num,path,component,
        is_frame,menu_type,visible,
        status,perms,icon,
        create_by,create_time,update_by,
        update_time,remark,del_flag
    </sql>


    <select id="getPermsById" resultType="java.lang.String">
     select  perms
     from sys_menu m
                   right join sys_role_menu  r_m
                       on  r_m.menu_id=m.id
                   where  r_m.role_id=#{id}
     </select>
    <select id="getAllPerms" resultType="java.lang.String">
        select  DISTINCT  perms
        from  sys_menu m
        where  menu_type in ("C","F")  and
        status=0
    </select>
    <select id="getAllMeus" resultType="cn.wenzhuo4657.blog.admin.domain.enity.Menu">
        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component, m.visible, m.status,
                     IFNULL(m.perms,'') AS perms, m.is_frame,  m.menu_type, m.icon, m.order_num, m.create_time
        FROM
            `sys_menu` m
        WHERE
            m.`menu_type` IN ('C','M') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id,m.order_num
    </select>
    <select id="getById" resultType="cn.wenzhuo4657.blog.admin.domain.enity.Menu">

        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component, m.visible, m.status,
                     IFNULL(m.perms,'') AS perms, m.is_frame,  m.menu_type, m.icon, m.order_num, m.create_time
        FROM
            `sys_user_role` ur
                LEFT JOIN `sys_role_menu` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `sys_menu` m ON m.`id` = rm.`menu_id`
        WHERE
            ur.`user_id` = #{userId} AND
            m.`menu_type` IN ('C','M') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id,m.order_num
    </select>
</mapper>
